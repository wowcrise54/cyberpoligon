---
- name: Verify ping
  hosts: test_debian
  gather_facts: false

  tasks:
    - ping:
      register: pong
      tags: always

    - ansible.builtin.assert:
        that: pong.ping == "pong"
      tags: always
